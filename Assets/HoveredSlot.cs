using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HoveredSlot : MonoBehaviour
{
    public int hoveredSlot;
    // Start is called before the first frame update
    void Start()
    {
        hoveredSlot = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
